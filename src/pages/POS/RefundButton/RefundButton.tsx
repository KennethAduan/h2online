import { Button } from "@mui/material";
const RefundButton = () => {
  return (
    <div>
      <Button variant="outlined" fullWidth size="large" sx={{ width: 300 }}>
        REFUND
      </Button>
    </div>
  );
};

export default RefundButton;
